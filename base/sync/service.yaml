apiVersion: v1
kind: Service
metadata:
  name: sync-service
  namespace: services
spec:
  # selector:
  #   app: backend-report-service
  ports:
    - name: http
      protocol: TCP
      port: 9002
      # targetPort: backend-report
  type: ClusterIP
---
